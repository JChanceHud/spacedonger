fixed typos in docs

0.8.7:

fixed: multitouch events did not work on actor states

0.8.6:

fixed critical bug that prevented collision handlers from working
device orientation (tilt) support including demo
introducing gamvas.math for miscellaneous math functions

0.8.5:

actors created with new Actor(false, 1, 2) or new Actor() now get a autogenerated unique name

a* pathfinding, grid and node based + demos
multi touch support + demo
experimental fullscreen support (firefox only)
changed physics listener, not altering box2d listener anymore

new functions
gamvas.event.stopBubble();
gamvas.Class.objectID();
gamvas.state.getState(name);

bugfix: onKeyUp/onKeyDown character now works
bugfix: gamvas.mouse.isPressed() now working
bugfix: handle config.preventKeyEvent and config.preventMouseEvent correct

gamvas.Vector2D improvements:
- gamvas.Vector2D.copy();
- gamvas.Vector2D.subtract(v)
- gamvas.Vector2D.add(v)
- gamvas.Vector2D.difference(v)
- gamvas.Vector2D.distance(v)
- gamvas.Vector2D.quickDistance(v)

0.8.4:

new demo: Player controlled physics objects
new actor functions:
    setAngularDamping();
    setLinearDamping();
demos now ignore key events to prevent browser from scrolling by pressing cursor keys
bugfix: Box2D javascript files were required even if Box2D was disabled
bugfix: gamvas.physics.resetWorld now actually handles parameters it gets
bugfix: moving kinematic objects via move/setPosition rotate/setRotation is now possible

0.8.3:

use one physics world per state (gamvas.config.worldPerState to control)
bugfix: layer order was wrong direction (gamvas.config.reverseLayerOrder to control)
draw loading screen without camera applied
changed default of keyboard/mouse handling to not interfere with the browser
gamvas.State.removeActor()
bugfix: gamvas.physics.resetWorld() now resets debug draw too
bugfix: gamvas.State leave() not called when initializing first state
fixed several errors and/or typos in docs

0.8.2:

gamvas.actor.setFixedRotation() now works
gamvas.actor now puts onMouse* and onKey* events through to the current actorstate
translated TODO list, just in case someone wants to help ;)
config variables:
- gamvas.config.preventKeyEvents (true/false)
- gamvas.config.preventMouseEvents (true/false)

0.8.1:

dropped gamvas.physics.debugFill
dropped gamvas.physics.debugStroke
introduced gamvas.physics.debugAlpha
now use official box2d debug drawing

changed event system to give all input events to the game instead the browser
new class: gamvas.Rect

actor.init();
actor.setGroupIndex();
actor.setCategoryBits();
actor.setMaskBits();

gamvas.Image.setClipRect()
gamvas.Image.getClipRect()

gamvas.animation.setFrameList()
gamvas.actor.setFrameList();
gamvas.actor.setFPS();
gamvas.actor.addRevoluteJoint()

grafik noch machen: parallax scrolling demo
evtl wieder loeschen: physics joint demo
particle demo
